import requests
import config


def do_fetch() -> bool:
    return True
    # url = config.BASE_URL + 'fetch_status?group_id=' + config.GROUP_ID
    # result = requests.get(url).text
    # return result == 'True'
